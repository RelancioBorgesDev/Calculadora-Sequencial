//Classe Node que "simula" um nó em uma Linked List
class Node{
	//Chamada do construtor que recebe o valor e o ponteiro (element, next)
	constructor(element, next){
		this.element = element;
		this.next = next;
	}
}

export default Node;